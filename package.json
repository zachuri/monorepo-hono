{
	"name": "hono-monorepo",
	"version": "0.1.0",
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"private": true,
	"scripts": {
		"api": "bun run --cwd ./apps/api dev",
		"check-deps": "check-dependency-version-consistency .",
		"lint": "turbo lint",
		"format": "turbo format",
		"build": "turbo build"
	} 
}
